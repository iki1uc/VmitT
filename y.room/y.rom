<div class="room-y">
  <h2>Y‑Raum</h2>
  <p>Punkt / Node / Position</p>
</div>

async function loadRoom(file){
  const html = await (await fetch(file)).text();
  document.getElementById("room").innerHTML += html;
}
